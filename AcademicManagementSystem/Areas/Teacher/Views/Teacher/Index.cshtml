@model AcademicManagementSystem.ViewModels.TeacherHomeVM
@{
    ViewData["Title"] = "My Courses";
}
<h2>Welcome, @Model.TeacherFullName</h2>
<hr/>
<br/>
<h2>My Courses</h2>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Title</th>
            <th>Semester</th>
            <th>Programme</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var c in Model.Courses)
        {
            <tr>
                <td>@c.Title</td>
                <td>@c.Semester</td>
                <td>@c.Programme</td>
                <td>
                    <a class="btn btn-sm btn-primary"
                       asp-area="Teacher"
                       asp-controller="Teacher"
                       asp-action="Course"
                       asp-route-id="@c.CourseId">
                        View
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
